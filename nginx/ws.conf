# ws.conf
server {
    server_name osrs-music-api.stevenwaterman.uk;
    root /var/www/html/web;
    location ^~ /.well-known/acme-challenge/ {
       log_not_found off;
       access_log off;
       allow all;
    }
    location /index.html {
       log_not_found off;
       access_log off;
       allow all;
    }
    location /create {
       proxy_pass http://websocket/create;
       proxy_http_version 1.1;
       proxy_set_header Upgrade $http_upgrade;
       proxy_set_header Connection $connection_upgrade;
    }
    location /join {
       proxy_pass http://websocket/join;
       proxy_http_version 1.1;
       proxy_set_header Upgrade $http_upgrade;
       proxy_set_header Connection $connection_upgrade;
    }

    listen 443 ssl; # managed by Certbot
    ssl_certificate /etc/letsencrypt/live/osrs-music-api.stevenwaterman.uk/fullchain.pem; # managed by Certbot
    ssl_certificate_key /etc/letsencrypt/live/osrs-music-api.stevenwaterman.uk/privkey.pem; # managed by Certbot
    include /etc/letsencrypt/options-ssl-nginx.conf; # managed by Certbot
    ssl_dhparam /etc/letsencrypt/ssl-dhparams.pem; # managed by Certbot
}

server {
    if ($host = osrs-music-api.stevenwaterman.uk) {
        return 301 https://$host$request_uri;
    } # managed by Certbot

    server_name osrs-music-api.stevenwaterman.uk;
    listen 80;
    return 404; # managed by Certbot
}