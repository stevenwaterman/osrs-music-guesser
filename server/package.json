{
  "name": "osrs-music-guesser-server",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "concurrently --kill-others \"cd ../shared && yarn run dev\" \"tsx watch src/index.ts --clear-screen=false\"",
    "build": "tsc --build"
  },
  "devDependencies": {
    "@types/node": "^20.11.20",
    "@types/ws": "^8.5.10",
    "concurrently": "^8.2.2",
    "tsx": "^4.7.1",
    "typescript": "^5.2.2"
  },
  "dependencies": {
    "osrs-music-guesser-shared": "1.0.0",
    "ws": "^8.16.0"
  }
}
