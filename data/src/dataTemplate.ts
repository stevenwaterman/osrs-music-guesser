// This file is used as a template for the data sub-project, which compiles data from multiple sources into a massive JSON blob automatically and saves it to data.ts
// This is just an optimisation so that it happens once at compile time instead of happening on each client at runtime.

export const songsInternal = /*replace*/{}/*replace*/ as const;
